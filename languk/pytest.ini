[pytest]
DJANGO_SETTINGS_MODULE = languk.settings.e2e
testpaths = e2e
python_files = test_*.py
addopts = --browser chromium
markers =
    live_data: runs only against a populated server (set E2E_BASE_URL)
